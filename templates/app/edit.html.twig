{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h2>Modifier le livre</h2>

        {% include 'app/form.html.twig' %}
        <button class="addKeyword btn btn-info">Ajouter Mot Clé</button>

        <div class="row mt-3">
            <div class="col-sm-12">
                <button type="submit" class="btn btn-success">Modifier l'annonce</button>
                {{ form_end(form) }}
                </form>

                <a href="{{ path('home') }}" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Annuler</a>
            </div>
        </div>
    </div>

{% endblock body %}

{% block javascripts %}

    {{ parent() }}

    <script src="{{ asset('js/form_car.js') }}"></script>

{% endblock javascripts %}