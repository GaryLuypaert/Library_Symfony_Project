{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h2>Ajouter un bouquin :</h2>

        {% include 'app/form.html.twig' %}
        <button class="addKeyword btn btn-info"><i class="fas fa-plus"></i> Ajouter Mot Clé</button>

        <div class="row mt-3">
            <div class="col-sm-12">
                <button type="submit" class="btn btn-success"><i class="fas fa-check"></i> Ajouter l'annonce</button>
                {{ form_end(form) }}
                </form>

                <a href="{{ path('home') }}" class="btn btn-primary"><i class="fas fa-arrow-left"></i> Annuler</a>
            </div>
        </div>

    </div>

{% endblock body %}

{% block javascripts %}

    {{ parent() }}

    <script src="{{ asset('js/form_car.js') }}"></script>

{% endblock javascripts %}